{
  "name": "at-test",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "artillery version",
    "quick": "artillery quick --insecure https://eventx.io -o test.json",
    "report": "artillery report",
    "dino": "artillery run --config common-config.yml scenarios/dino.yml -o dino.json"
  },
  "devDependencies": {
    "artillery": "^2.0.0-26"
  }
}
